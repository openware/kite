provider "google" {
  credentials = file("<%= @cloud['gcp']['service_account'] %>")
  project     = "<%= @cloud['gcp']['project'] %>"
  region      = "<%= @cloud['region'] %>"
}

terraform {
  required_version = ">= 0.12"
}
