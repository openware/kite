---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: heliostech/kite

inputs:
- name: pull-requests

outputs:
- name: tag

run:
  path: sh
  args: ['-exc', 'cd pull-requests && git rev-parse --short=8 HEAD > ../tag/name']